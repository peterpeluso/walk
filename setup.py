from setuptools import setup

setup(name='walk',
      version='0.0.1',
      description='Simple package for random walks',
      url='',
      author='',
      author_email='',
      license='',
      install_requires=[
          'matplotlib','numpy', 'pandas'
      ],
      packages=['walk'],
      zip_safe=False)
